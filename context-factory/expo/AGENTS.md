# Agent Rules Configuration

The rules for this project are located in:
`context-factory/expo/rules/`

One file per rule category (e.g., `mutation-hooks.md`, `project-structure.md`, `query-hooks.md`, `zustand-store.md`).

## Applying Rules

The agent should natively check the `context-factory/expo/rules/` directory for active rules.
