# Goal

- To fully implement on how the mediapipe works on sentinel-web
    - Ensure that 1:1 functionalities on the sentinel-web to sentinel-mobile

    From checkup pages for configuring the mediapipe 
    From lobby page that won't allow the student to enter unless it was configured 
    Detecting GAZE_OFF_SCREEN, MULTIPLE_FACES, etc
    Automatic live capture frame 
    Audio anomaly detection

- After examination / attempt page that should redirect to the feedback page similarly on the sentinel-web
- The reconnection attempt that should redirect back to the lobby if something happens
- Ensure that when the instructo checks the live monitoring the livekit can access the camera of the student
- Ensure that the lobby page count the stuident on the lobby page similar on the sentinel-web
= Ensure that the checkup page configure similar on the sentinel-web where the person should be at the center and wait to configure 
- Ensure the attempt page has a passage similarly on the sentinel-web
- Fix the attempt page where the questions are not showing
- Ensure the after turning in it will navigate to the feedback page similar on the sentinel-web
- Overall ensure that all the functionalities, experience on the sentinel-web during the examination it should be implemented on the sentinel-mobile